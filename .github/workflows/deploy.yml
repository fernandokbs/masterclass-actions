name: Deploy to server

on:
  push:
    branches:
      - main

jobs:
  set-up:
    runs-on: ubuntu-latest

    steps:
     - name: setup
       run: echo "setup"

  deploy:
    if: false
    needs: set-up
    runs-on: ubuntu-latest

    steps:
     - name: Hola Mundo
       run: echo "HOLA MUNDO"
  